export default function StaffDetailPage({ params }: { params: { id: string } }) {
  return (
    <div>
      <h1>Chi tiết nhân viên {params.id}</h1>
      {/* TODO: Implement staff detail view */}
    </div>
  );
}